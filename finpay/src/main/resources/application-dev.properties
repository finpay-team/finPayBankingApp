# ==============================================================================
# Development Profile
# ==============================================================================

# Database - Use Neon cloud DB for dev (or override via env)
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-curly-band-aoskeknb.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_zsv5AcyiuLg9}

# JPA - Show SQL in dev
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging - Verbose in dev
logging.level.root=INFO
logging.level.com.finpay=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# DevTools
spring.devtools.restart.enabled=true